import { Bot } from './src/bot';

/**
 * At the moment, this is the only thing needed to start the bot, but that's because an api will be created later on,
 * allowing the bot to use arguments.
 */
new Bot().start();